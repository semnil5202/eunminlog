{
  "name": "eunminlog",
  "private": true,
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.6.2",
  "scripts": {
    "setup:local": "pnpm --filter @eunminlog/client setup:local && pnpm --filter @eunminlog/admin setup:local",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  },
  "devDependencies": {
    "prettier": "^3",
    "turbo": "^2"
  }
}
